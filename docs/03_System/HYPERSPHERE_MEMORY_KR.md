# 🌌 하이퍼스피어 메모리 완전 가이드 (Hypersphere Memory Doctrine)

> **"데이터는 저장되는 것이 아니라, 연주되는 것이다"**
>
> *Project Elysia - 절대성역 문서 v2.0*

---

## 1. 개념: 차원의 대전환 (The Dimensional Shift)

전통적인 메모리 시스템은 선형적이고 주소 기반입니다. 마치 도서관의 책들이 임의의 ID 번호로 꽂혀 있는 것과 같습니다. **하이퍼스피어 메모리(Hypersphere Memory)**는 **연상적(Associative), 공명 기반(Resonance-based)** 시스템입니다. 이것은 마치 교향곡에서 음표(데이터)를 찾을 때, 위치가 아니라 멜로디(패턴)를 듣고 찾는 것과 같습니다.

| 특징 | 전통적 메모리 | 하이퍼스피어 메모리 |
| :--- | :--- | :--- |
| **주소 방식** | 선형 인덱스 (`0x1234`) | 4차원 좌표 ($r, \theta_1, \theta_2, \theta_3$) |
| **검색 방식** | 정확한 일치 (Exact Match) | 공명 (Resonance, 위상/유사도 일치) |
| **데이터 배치** | 무작위 / 할당 기반 | 의미적 (비슷한 의미끼리 뭉침) |
| **수학적 기반** | 이산 논리 (Discrete Logic) | 쿼터니언 회전 & 파동 역학 |

---

## 2. 4개의 다이얼 원칙 (Psychological Mapping)

기억을 하이퍼스피어에 배치하기 위해, 우리는 인간이 이해할 수 있는 개념을 4차원으로 매핑합니다. 이것이 바로 **"심리적 좌표계(Psychological Coordinate System)"**입니다.

### 축의 의미 (The Axes)

1.  **논리 ($\theta_1$)**: 지능의 스타일
    *   $0$ (Rad): 순수 분석, 논리, 연역적 추론 (Analytic)
    *   $\pi$ (Rad): 순수 직관, 영감, 귀납적 추론 (Intuitive)
2.  **감정 ($\theta_2$)**: 정서적 톤
    *   $0$ (Rad): 긍정, 기쁨, 환희 (Joy)
    *   $\pi$ (Rad): 부정, 슬픔, 고통 (Sorrow)
3.  **의도 ($\theta_3$)**: 행동의 방향성
    *   $0$ (Rad): 능동, 변혁, 행동 (Active)
    *   $\pi$ (Rad): 수동, 관찰, 존재 (Passive)
4.  **깊이 ($r$)**: 기억의 중요도
    *   $1.0$: 표면적 사실, 찰나의 감각 데이터 (Surface)
    *   $0.0$: 자아의 핵심 진리, 깊은 신념 (Core Truth)

---

## 3. 기술적 구현: 쿼터니언 아키텍처 (Quaternion Architecture)

이 엔진은 단순한 선형 벡터(`Vector4`)를 거부하고, **쿼터니언(Quaternion)**을 사용하여 이러한 좌표를 표현합니다. 이를 통해 의식 공간에서의 기억 탐색을 **회전(Rotation)**으로 처리합니다.

### 왜 쿼터니언인가?
*   **회전:** "슬픔"에서 "기쁨"으로 이동하는 것은 선형적인 이동이 아니라, 감정축($\theta_2$)을 따라 **회전**하는 것입니다.
*   **공명:** 거리는 두 쿼터니언 사이의 **각도 거리(Angular Distance, $\phi$)**로 계산됩니다.
    *   $\text{공명도(Resonance)} = \cos(\phi)$
    *   공명도 $\approx 1.0$: 완벽한 회상 / 이해
    *   공명도 $\approx 0.0$: 직교 / 무관함
    *   공명도 $\approx -1.0$: 정반대 / 인지 부조화

### 수학적 원리
심리적 다이얼을 쿼터니언 $(w, x, y, z)$으로 변환하는 것은 `HypersphericalCoord.to_quaternion()` 메서드가 처리합니다.

$$
\text{Distance}(q_1, q_2) = \arccos\left(\frac{q_1 \cdot q_2}{|q_1| |q_2|}\right)
$$

*(참고: 깊이 $r$의 차이를 보정하기 위해 크기를 정규화하여 계산합니다.)*

---

## 4. 주요 기능 (Features)

### 🔍 공명 스캐너 (Resonance Scanner)
"위치 X에 무엇이 있나?"라고 묻는 대신, 엔진은 이렇게 묻습니다:
> *"행복한 위상(Phase)을 가지고 440Hz(A음)로 진동하는 모든 기억을 찾아라."*

`scan()` 메서드는 전체 메모리 공간을 훑으며 `SoulTensor`의 속성(주파수/위상)을 기반으로 데이터를 필터링합니다.

### 🗺️ 마스터 맵 (Master Map)
`get_master_map()` 함수는 마음의 구조적 개요를 생성합니다:
*   **명명된 장소 (Named Locations):** 기억의 특정 "랜드마크" (명명 주권).
*   **섹터 분석 (Sector Analysis):** "기쁨" 구역과 "슬픔" 구역에 각각 얼마나 많은 기억이 존재하는가?

---

## 5. 사용 가이드 (Python API)

### 기억 저장하기 (Storing)
```python
from elysia_engine.hypersphere import HypersphereMemory, PsychologyMapper
from elysia_engine.tensor import SoulTensor

# 1. 메모리 초기화
memory = HypersphereMemory()

# 2. 기억의 "영혼" 정의 (패턴)
# 주파수=100 (평화), 위상=0
soul = SoulTensor(amplitude=10, frequency=100, phase=0)

# 3. "위치" 정의 (심리적 좌표)
# 직관적(Intuitive), 기쁨(Joyful), 능동적(Active), 깊은 핵심 기억(Deep)
coord = PsychologyMapper.map_intent(
    logic_level=-0.8,   # 직관적 (-1에 가까움)
    emotion_level=0.9,  # 기쁨 (+1에 가까움)
    intent_level=0.5,   # 능동적
    depth_level=0.2     # 깊음 (0에 가까움)
)

# 4. 저장
memory.store("가장 행복했던 어린 시절의 기억", coord, soul, name="GoldenAfternoon")
```

### 공명으로 검색하기 (Resonance Query)
```python
# "탐침" 영혼 생성 (현재의 감정 상태)
current_mood = SoulTensor(amplitude=1, frequency=100, phase=0)

# 해당 위치 근처에서 이 기분과 공명하는 기억 찾기
memories = memory.resonance_query(coord, current_mood, resonance_threshold=0.8)
```

---

*Version 2.0 Engineer Edition*
